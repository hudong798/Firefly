# Design Context — 大海星辰 (Firefly)

> 本文件由 impeccable 设计技能生成，为后续所有 UI 工作提供持久上下文。
> 修改界面之前先读这里，不要从代码反推设计意图。

## Design Context

### Users

**公开访客，无需登录**（谁都能看）。三类人：

1. **技术同好**——从搜索引擎或友链进来，想快速判断"这个博客值不值得读"，然后读一篇文章就走。
2. **熟人/朋友**——直接进来找最近写了什么，顺手看看影视和想法区。
3. **未来的自己**——通过归档和时间轴回看旧文。

使用情境：碎片时间、多标签页、深浅色取决于环境光（白天通勤看浅色、深夜看深色）。
**任务**：找内容 → 判断相关性 → 读 → 找到下一篇。登录、评论、留言都是次要动作。

### Brand Personality

**清透 · 有层次 · 被光穿过**（Clear · Layered · Lit through）

站点名"大海星辰"是核心意象：海面、雾气、星尘。语气是**安静的、有品味的、不喧哗的**——像夜里一个人看着水面，不是夜店镭射灯。

情绪目标：**信任感 + 安静的好奇**。访客应该觉得"这个人有耐心，东西是认真做的"。

### Aesthetic Direction

**方向**：毛玻璃物质主义（Glass Materialism）。玻璃是**材质**，不是滤镜贴纸。

**核心洞察**：玻璃只有背后有东西可以折射时才成立。纯色背景上的毛玻璃等于一块半透明的白/灰方块——这就是浅色主题下大量毛玻璃设计崩坏的原因。因此本设计的第一个决定是**先建光场，再放玻璃**。

**参考**：iOS/macOS 的 vibrancy 材质系统（层次一致、单一光源）、Swiss 编辑设计（字号对比大、留白狠）、Apple Music 的沉浸式背景。
**反参考（明确不要）**：
- 紫蓝渐变 + 霓虹描边的"AI 默认深色"
- 万物皆玻璃——当每张卡片都是玻璃时，玻璃失去了"浮起"的意义
- 玻璃面板上放 14px 浅灰次要文字（对比度必挂）
- 居中一切 + 大图标 + 渐变大标题的落地页模板
- 回弹/弹跳缓动、入场动画表演

**主题**：深色（默认，夜间/沉浸）+ 浅色（暖白雾面，白天），双主题等价支持，不是附加品。

### Design Principles

1. **背后有东西，玻璃才成立。** 凡用玻璃处，其下必有可折射的光场或内容。纯色区域、承载正文的区域——退回实心面。玻璃面板占比不超过可视面积的 40%。

2. **一个光源，一种材料。** 全站高光方向固定在左上（`135deg` 系），投影落在右下。所有玻璃共享同一套边缘公式（顶部 1px 内高光 + 底部 1px 内暗边 + 外投影）。一致性是"像真材料"的关键。

3. **正文优先于效果。** 承载文字的容器用实心（`--solid-*`），保证正文 4.5:1、大标题 3:1。玻璃只用于：导航、浮层、卡片**外壳**、侧栏 widget。次要文字在玻璃上也必须实测过对比度，不能靠眼睛估。

4. **浅色不是深色反相。** 深色靠"越靠上越亮的面 + 内高光"表达层级；浅色靠"投影 + 更实的白面"表达层级。深色下正文降 weight、行高 +0.05；浅色下强调色加深一档以补对比。

5. **背景缓慢，内容安静。** 光场以 48s+ 周期漂移（只动 `transform`），读的时候感知不到它在动，但停顿看时会发现"它是活的"。动效只表达状态变化，不做入场表演；全站尊重 `prefers-reduced-motion`。

### 工程约束（决定实现取舍）

- 目标机型包含 **Intel 核显 + 8GB 内存**，所以：`backdrop-filter` 只用于导航/浮层/卡片外壳，模糊半径 ≤ 24px，**禁止嵌套** backdrop-filter（每个玻璃层都会强制一次独立的离屏合成，嵌套会指数级变慢）；背景光斑用纯 `radial-gradient` 而非 `filter: blur()`，避免额外合成层。
- 用户主题色选择器依赖 `--hue`（当前 265），所有颜色必须写成 `var(--hue)` 参数化形式，不能硬编码色相。
- 站点是 Astro + Svelte + Tailwind 4 / Stylus，令牌需同时能被 CSS 变量和 Tailwind 主题消费。
